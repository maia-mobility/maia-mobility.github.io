# MAIA Lab 웹사이트

명지대학교 MAIA 연구실(Mobility, AI & Autonomous System, PI 유화평) 홈페이지.
기존 Wix 사이트(`https://yhp7237.wixsite.com/mysite`)를 대체한다.

**스택**: Astro 7 + React 19 + TypeScript. 정적 빌드. 애니메이션은 Canvas 2D 직접 구현
(three.js 등 외부 라이브러리 없음).

---

## 디자인 언어 — 타협 불가

시각 언어는 **LiDAR로 스캔한 정밀도로지도(HD map point cloud)** 다.
톤은 *계측 장비의 HUD*이지 *SaaS 랜딩페이지*가 아니다.

1. **`border-radius: 0`** — `global.css` 전역 가드 1곳이 유일한 등장 지점. 예외 없음.
   캔버스 점도 `arc()` 가 아니라 `fillRect` 정사각.
2. **`box-shadow` 로 띄운 카드 금지.** 깊이는 헤어라인 + 코너 틱 + 배경 광도 단계 + 캔버스 글로우로만.
3. 카드 = `HudFrame` (선택된 변에만 1px + 코너 틱).
4. 버튼 = 사각 1px, 모노 대문자, `letter-spacing: .12em`, `▸`. hover 시 **테두리가 점등**(배경 X).
5. 그라디언트 메시·블러 블롭 금지. 빛나는 것은 캔버스뿐.
6. 섹션 구분선은 실선이 아니라 점선 스캔 라인.
7. 아이콘은 둥근 SVG가 아니라 1px 스트로크 기술 기호.
### 한글 굵기

같은 `font-weight` 에서 한글은 라틴보다 가늘어 보인다 — 한 글자당 획이 많아
글자 속공간이 먼저 메워지고 획이 상대적으로 가늘게 지각된다. `:lang(ko)` 에서
굵기 토큰을 한 단계씩 올린다(thin 200→300, light 300→400, regular 400→500).
Apple SD Gothic Neo 는 정적 패밀리라 **100 단위로만** 올린다(350 은 반올림된다).

`--w-mono` · `--w-mono-em` · `--w-head` 는 **언어와 무관하게 고정**이다 —
모노 계측 표기는 라틴이라 보정이 필요 없고, 큰 헤딩은 한글에서 이미 충분히 두껍다.

8. **색상 하드코딩 금지** — 전부 `src/styles/global.css` 의 토큰(`var(--…)`).

시각 작업은 `maia-design` 서브에이전트(`.claude/agents/maia-design.md`)에 위임한다.
그 파일이 토큰·절차·체크리스트의 원본이다.

### 감사 명령

```bash
grep -rn "border-radius" src/                                       # 전역 가드 1곳 외 → 실패
grep -rnE "box-shadow|blur\(|linear-gradient|radial-gradient" src/  # 정당화 필요
grep -rnE "#[0-9a-fA-F]{3,8}" src/ | grep -v styles/global.css      # 결과 있으면 실패
grep -rnE "\.arc\(" src/hooks src/components                        # 캔버스 원형 점 → 실패
```

---

## React 규칙

- **rAF 루프가 React state 를 건드리지 않는다.** 프레임당 `setState` 는 버그로 취급한다.
  애니메이션은 `useRef` 로 잡은 캔버스에 직접 그린다.
  state 는 시나리오 전환·필터·메뉴 개폐 등 UI 이벤트에만.
- **아일랜드 최소화.** 정적 컴포넌트는 `client:` 지시어 없이 HTML 로만 렌더 → JS 0KB.
  하이드레이션 대상만 지시어를 붙인다:
  `PointCloudHero client:load` · `TrafficSim client:visible` ·
  `Nav`/`LangToggle` `client:idle` · `DotPortrait client:visible`.
- 애니메이션 로직은 `src/hooks/` 의 커스텀 훅이 소유한다(씬 생성·rAF·리사이즈·정리 전부).
  컴포넌트는 `<canvas ref>` 와 마크업만 렌더한다.
- 모든 루프는 `useRafLoop` 을 거친다 — 화면 밖·`document.hidden`·reduced-motion 시 자동 정지.

### 기반 훅 (이미 있음 — 새로 만들지 말 것)

| 훅 | 역할 |
|---|---|
| `useRafLoop(cb, {active, target, reducedMotion, fps, maxDelta})` | 자동 정지되는 rAF 루프. state 미사용 |
| `useCanvas2D(ref, {maxDpr, onResize})` | DPR 스케일링 + ResizeObserver. `{ctx, width, height, dpr}` ref 반환 |
| `usePrefersReducedMotion()` | OS 동작 줄이기 설정 (`useSyncExternalStore`, SSR 안전) |
| `useInView(ref, {once})` | 등장 연출용 IntersectionObserver |

---

## i18n

영문 기본(`/`) + 한글(`/ko/`). `astro.config.mjs` 의 `prefixDefaultLocale: false`.

- 모든 콘텐츠는 `{ en, ko }` (`I18nText`) 쌍. 번역 불필요한 값은 `mono()` 로 감싼다.
- 컴포넌트는 `lang` prop 을 받아 `t(lang)(value)` 또는 `pick(value, lang)` 으로 고른다.
- 링크는 반드시 `href(routeKey, lang)` 으로 만든다 — 하드코딩 금지.
- 페이지 추가 시 `src/i18n/ui.ts` 의 `NAV` 와 `META` 양쪽에 등록한다.

## 콘텐츠 추가

| 하려는 것 | 고칠 파일 |
|---|---|
| 논문 추가 | `src/data/publications.ts` — 배열 맨 앞에 한 항목 |
| 뉴스 추가 | `src/data/news.ts` |
| 구성원 추가 | `src/data/people.ts` 의 `MEMBERS`. 사진은 원본을 `pic/`(git 무시)에 두고 `python3 scripts/fit-portrait.py pic/원본 public/images/people/<id>.jpg --face 0.53 --eye 0.34 --bg '#f3f2f0'` 로 낸 사본만 넣는다 — 얼굴 폭·눈높이·배경색이 전원 같아진다(macOS Vision 얼굴·인물 분리, 로컬) |
| 강의 추가 | `src/data/teaching.ts` |
| 연구분야 수정 | `src/data/research.ts` |
| 연락처·주소 | `src/data/site.ts` |

> **연구분야에는 설명문을 두지 않는다** (교수님 결정). 제목 + 키워드 칩 + 실제 IDM 교통
> 시뮬레이션이 설명을 대신한다. 산문을 새로 지어 넣지 말 것.
> `SIM_LEGEND` 는 연구 설명이 아니라 시뮬레이션 화면의 계측 표기(HUD)다.

## 에셋

`_archive/wix-images/` 는 기존 사이트의 DALL-E 생성 스톡 이미지다(깨진 글자 포함).
**빌드에 넣지 말 것.** 연구분야는 이미지 대신 실제 IDM 교통 시뮬레이션으로 표현한다.
`public/images/pi.jpg` 만 진짜 사진이라 사용한다.

---

## 개발

개발 서버는 백그라운드 모드로 띄운다:

```
astro dev --background
```

`astro dev stop`, `astro dev status`, `astro dev logs` 로 관리.

## 검증 — 추측하지 말고 측정한다

```bash
npm run verify      # 타입검사 → 디자인 하드 룰 → 빌드 → 브라우저 실측 (개발 서버 필요)
```

개별 도구 (전부 실제 Chrome 을 띄워 잰다):

| 명령 | 무엇을 재는가 |
|---|---|
| `npm run typecheck` | 타입 오류 |
| `npm run lint:design` | 하드 룰 위반 (radius·shadow·gradient·하드코딩 색·arc·rAF 내 setState) |
| `npm run audit` | 12페이지 × 5폭 가로 스크롤 · h1 개수 · 캔버스 aria-hidden · 내비 전환 |
| `npm run hero:check` | 히어로 캔버스가 실제로 그려지는지 (픽셀 검사) |
| `npm run stage:check` | 연구 무대 — 시점이 **거기서만** 오르내리는지 · 구역을 지나면 그 막이 서는지 · 막이 01→04 순서로 오는지 · 차간이 불규칙한지 · 옆 차로에 stop-and-go 가 있는지 · 보이는 시뮬 **하나만** 도는지 |
| `npm run sim:check` | 4개 시뮬레이션의 HUD 수치를 페이지에서 읽어 검증표와 대조 |
| `npm run sim:trace` | 플래툰 지표가 사이클 안에서 어떻게 변하는지 추적 |
| `npm run og` | 미리보기 이미지 재생성 (프로덕션 빌드 기준) |

**`stage:check` 는 한 막을 11걸음에 나눠 지난다.** 걸음을 줄이지 마라 — 배경의
시뮬 시각은 주행거리로 흐르므로, 성큼성큼 지나가면 승객이 타기도 전에 다음 막이다.

**측정 도구를 믿되, 타이밍을 의심하라.** 시뮬레이션 지표는 정착까지 시간이 걸린다 —
플래툰은 45초 사이클의 후반부, 링 도로는 AV 1대 기준 약 70초. 과도구간의 값을
정착값으로 착각하면 연구 결론이 뒤집혀 보인다 (실제로 한 번 그랬다).

빌드 후 눈으로 확인할 것:
- 산출 HTML 에 논문 제목·교수님 성함이 **텍스트로** 들어있는가 (아일랜드 남용 여부)
- 논문/People 페이지의 JS 번들이 0 인가 (`_astro/*.js` 참조가 없어야 한다)

## 배경 도로와 본문의 계약

배경 캔버스(`usePointCloud`)와 본문 마크업은 **DOM 속성과 CSS 커스텀 프로퍼티**로만 만난다.
둘 사이에 React state 도 이벤트도 없다.

| 방향 | 이름 | 뜻 |
|---|---|---|
| 마크업 → 훅 | `[data-road-stage]` | 연구 무대 |
| 마크업 → 훅 | `[data-act]` | 무대 안의 한 화면. 막 진행도를 **이 요소의 세로 위치에서** 잰다 |
| 훅 → CSS | `--h` | 히어로 카피 퇴장 0→1. 자는 **여는 화면의 실제 길이**다(무대 상단의 문서 y − 0.15H) |
| 훅 → CSS | `--p` | 문서 전체 진행 0→1 |
| — | (완충) | `--h`·`--p`·`--v`·`--a*` 는 전부 **완충된 스크롤**(`SCROLL_TAU`)에서 나온다 — 본문 opacity 가 도로와 같은 시계로 움직인다 |
| 훅 → CSS | `--v` | 시점 0(대시캠 1.3m) → 1(루프탑 LiDAR 8.5m) |
| 훅 → CSS | `--a0`…`--a3` | 막별 진행도 0→1. 배경의 연출과 **같은 프레임**의 값이다 |
| 마크업 → 훅 | `[data-quiet]` | 본문이 앉는 자리. 그 화면 사각형 안에서 **점군이 성겨진다** |
| 마크업 → 훅 | `[data-ink]` | 여는 화면의 글자. 스크롤을 내리면 점으로 풀려 도로 위 차가 된다. 값 = 어느 차가 받는가(`lane` 앞차 대열 · `next` 옆 차로) |
| 훅 → 요소 | `--ink-at`·`--ink-k` | 그 글자가 풀리기 시작하는 `--h` 값과 그 구간의 역수. **베이크 때 한 번만** 쓴다 |

`data-quiet` 에는 CSS 가 붙지 않는다 — 순전히 캔버스에게 주는 표식이다.
캔버스 위 본문 가독성을 `backdrop-filter` 판으로 푼 적이 있는데, 대비는 7.9:1 까지
올라갔지만 결과가 **프로스티드 글래스 카드**였다(하드 룰 2·3 위반). 되돌렸다.
판을 다시 만들지 마라. 글이 안 읽히면 `QUIET_FLOOR`·`QUIET_SOFT` 를 조절한다.
화면 밖 사각형은 세로·**가로** 양쪽으로 걸러낸다 — 화면 밖으로 밀려난 요소까지
격자에 구우면 상한(`QUIET_MAX`)을 헛되이 쓴다.

값은 `<html>` 에 쓴다 — 도로는 `position: fixed` 배경이고 본문은 문서 흐름 안이라
형제 관계다. 한쪽 요소에만 쓰면 다른 쪽이 상속으로 읽지 못한다.
(`--ink-at`·`--ink-k` 만 예외로 그 글자 요소에 쓴다. 글자마다 값이 다르고,
프레임마다 바뀌지 않는다 — 베이크 때 한 번이다.)

### 첫 화면의 글자는 스캔 반사점이다

`[data-ink]` 글자를 한 글자씩 `Range` 로 재고 오프스크린에 같은 computed font 로
다시 그려 픽셀을 표본한다(`bakeGlyphs`). 그 점들이 스크롤(`hp`)을 따라 **앞차들의
차체 점군 자리**로 날아가 앉는다. 재생 헤드는 `hp` 하나뿐이고 새 시간축은 없다.

**첫 화면은 연출이 도는 동안 붙어 있다(sticky).** `.open` 안의 글 상자
(`.openStick`)가 `position: sticky` 로 화면에 머문다. 글자가 **보이던 그 자리에서**
점이 되어야 하기 때문이다 — 예전 구조는 "글자가 화면 위로 올라가다 점이 된다"였고,
교수님이 그 점을 지적했다("끝까지 올라가서 점으로 변하는 것보다 그 위치 그대로").

붙여 놓은 덕에 사라진 버그가 하나 더 있다. 예전에는 치환이 끝나는 시점을 **워드마크
상자의 문서 top** 이 정했는데, 세로가 짧은 창(1377×468)에서는 그 값이 96px 이라
치환 150px 을 앞에 둘 자리가 없어 일정이 통째로 눌렸다. 지금 일정은 글자 위치와
무관하게 **스크롤 px** 로만 정해진다: 위 구간표대로 스크롤 px 로만 정해진다. 어떤 창에서도 같다(모바일만 0.7배).

글자 점의 좌표도 문서가 아니라 **그 붙어 있는 상자 기준**이다(`Glyphs.dx`·`dy`).
상자의 화면 위치는 `data-quiet="ink"` 를 굽느라 이미 읽는 rect 에서 가져온다 —
프레임에 DOM 읽기를 더하지 않는다. 그 표식은 또 하나를 뜻한다: **글이 점으로 날아간
만큼 그 자리의 감쇠가 풀린다.** 붙어 있는 상자라 글이 사라져도 자리가 남으므로,
안 풀면 연출 내내 도로 왼쪽이 어둡다.

**일정은 전 요소가 하나다.** 다섯 줄(이름·확장명·태그라인·소개문·소속)이 같은 hp 에
치환을 시작해 같은 hp 에 끝내고 같은 hp 에 도착한다 — 첫 화면의 글이 **한 덩어리로**
점이 되었다가 위에서부터 풀린다. 요소마다 제 글자가 화면을 벗어나는 때에 맞춰
차례로 풀게 했더니 "순차적으로 바뀌어 어색하다"는 말을 들었다. 시차
(`GLYPH_STAGGER`)는 요소가 아니라 **첫 화면 전체의 세로 위치**로 준다.

**길이는 비율이 아니라 스크롤 px 다**: 시작 16 → 치환 250 → 풀어짐 150 → 조립 1,120
→ **도착 1,536px**. 모바일은 `GLYPH_MOBILE_K`(0.7)배라 도착 1,080px. 비율로 두면 창
크기마다 체감 속도가 달라진다.

**부드러움은 두 손잡이가 정한다.** ① 한 점의 비행 길이(`GLYPH_FLY_ONE_PX`) — 글자에서
차까지가 화면에서 900px 쯤이라 420px 짜리 비행이면 스크롤 1px 에 2.1px 씩 움직인다.
② **스크롤 입력 완충**(`SCROLL_TAU` 0.16s) + 재생 헤드 완충(`HP_TAU` 0.17s).

한때 이 자리에 "이 완충은 글자 연출만 쓴다, 도로에는 걸지 않는다"라고 적혀 있었다.
**틀렸다.** 글자는 매끄러워졌는데 화면 대부분인 도로는 휠 한 칸마다 계단으로 움직여
"휠 내릴 때마다 뚝뚝 끊기는 느낌"(교수님)이 그대로 남았다. 지금은 `scrollY` 를 한
곳에서 완충한 `st.scrollS` 가 **도로가 쓰는 모든 값**의 출처다: `camZ` 의 주행거리,
`p`, `hp`, 그리고 rect 에서 나오는 `approach`·`actT`(→ `riseK`·`actK`·`--a*`)까지
— rect 는 실제 스크롤 기준이라 `scrollLag`(= 실제 − 완충) 만큼 되돌려 쓴다.

**스크롤 하이재킹이 아니다.** 휠 이벤트를 가로채지 않는다 — 문서는 브라우저가
평소대로 굴리고, 도로가 그 값을 시간상수로 뒤따를 뿐이라 접근성도 네이티브 스크롤도
그대로다. "스크롤이 멈추면 도로도 멈춘다"도 지켜진다: 멈춘 뒤 0.5px 안으로 들어오면
**스냅**해서 정지 프레임 건너뛰기가 다시 선다(안 그러면 영영 안 선다).

실측(휠 한 칸 = 스크롤 100px, `scroll-behavior` 를 끄고 계단 그대로, 상승 구간):

| | 도로 최대 px/frame | Δrise 최대 | Δ`--a0` 최대 | 정착 |
|---|---|---|---|---|
| 완충 전 | 9.37 | 0.0548 | 0.3609 | 67프레임 |
| 완충 후 | **3.00** | **0.0070** | **0.0239** | 82프레임 |

실측(휠 한 칸 = `scrollBy(0,100)`, 조립 중간): 비행 80px·완충 없음이었을 때 글자 점의
**프레임당 이동량 평균 최대 98px · 12px 초과 689점**이었다. 지금은 **평균 최대 2.9px ·
단일점 최대 11.8px · 12px 초과 0점**, 전환이 18프레임에 퍼진다.

**연출은 두 단계다. 하나로 합치지 마라.**

| 단계 | 구간 | 무슨 일이 일어나는가 |
|---|---|---|
| ① 치환 | 앞 `GLYPH_HOLD_PX`(250px) | 점이 **글자 픽셀 위에 그대로** 켜지고(이동 0) DOM 글자가 같은 구간에서 0 이 된다. 끝에 **점으로 찍힌 첫 화면**이 선다. 점마다의 시차가 없다 — 글이 통째로 점이 된다 |
| ② 풀어짐 | 그 뒤 `GLYPH_LOOSEN_PX`(150px) | 점-글자가 **제자리에서** 흐트러진다(`GLYPH_DRIFT_X/Y`). 아직 아무도 떠나지 않는다. 세로는 아래로 처지되 **위로도 조금 번진다**(`GLYPH_DRIFT_UP`, 삼각분포) — 전부 아래로만 밀었을 때는 글 덩어리의 윗변이 직선으로 남아 태그라인·소개문 자리가 **잘라 붙인 직사각형**으로 보였다(교수님 스크린샷) |
| ③ 조립 | 그 뒤 `GLYPH_ASM_PX`(1,120px) | 점이 **줄지어** 떠나(한 점의 비행은 `GLYPH_FLY_ONE_PX` 420px) 차를 **차례로 쌓아 올린다** |

**수렴이 아니라 조립이다.** 한때는 비행의 마지막 32%에 전부 수렴시켰다. "도착 전
유령 없음"은 지켜졌지만 보통 스크롤 속도로는 그 100px 를 한 번에 지나쳐 **차가 되는
과정이 통째로 빠졌다**(교수님: "여기서 차량이 되는 과정이 아예 빠져 있어. 더 시각적
으로 강렬하게 차량이 되었으면 좋겠어"). 지금은 **출발을 조립 구간 전체에 흩뿌리고
개별 비행을 짧게** 잡는다. 그래서 어느 프레임을 잘라도 세 가지가 동시에 보인다:
글자 자리에 남은 점 · 공중을 흐르는 줄기 · 차에 앉아 굳은 점.

조립의 규칙 넷:

1. **차는 순서대로 쌓인다.** 무리 안에서 **가까운 차부터 가득** 채우므로(`share`는
   탐욕 배분) 화면에서 가장 큰 차가 가장 먼저·가장 오래 조립된다. MAIA 는 가장
   가까운 앞차를 통째로 만들고, 남는 점이 다음 차로 넘어간다.
2. **작고 많고 여러 겹이다.** 점은 공중에서 **1px 로 고정**이다 — `size·inv` 로
   보간했더니 가까운 차로 가는 점이 비행 중반에 4~6px 짜리 회색 사각형이 되어
   블록이 떠다니는 것으로 보였다("점들이 훨씬 작고 더 많이 퍼져서 압도되면
   좋겠어"). 차 한 대의 자리는 템플릿 점 ≈300 뿐이라 **한 자리에 `GLYPH_DUP_MAX`
   겹까지 내려앉고**, 여분은 닿기 직전(`cExt` 0.72→1)에 스러진다 — 넓게 퍼진
   무리가 차로 **압축**되는 것으로 읽힌다.
   점 수의 상한을 정하는 것은 예산이 아니라 **CPU 4배 스로틀의 드롭 0** 이다:
   글자 7,300점(총 17,285점)에서 p90 16.7ms, 11,000점(총 20,012)에서 p90 33.3ms
   로 프레임이 깨졌다. 상한을 올리려면 먼저 이 자를 다시 재라.
3. **한 차 안에서는 뒤에서 앞으로**(`CarTemplate.ordz`, 카메라에 가까운 면부터)
   스캐너가 훑듯이 쌓인다. 그래서 조립 중인 차는 흐릿한 전체가 아니라 **반쪽이
   또렷하고 반쪽이 없는** 모습이다.
4. **색은 앉는 순간에만 갈아탄다.** 공중의 점은 글자 색(흰색)이다 — 날아가는 도중에
   차체 색이 되면 "이미 차가 있다"로 읽힌다. 앉는 순간 `GLYPH_FLASH`(비행의 10%,
   비행의 15%) 동안 밝기가 1.2배로 한 번 튄다. 버킷은 **잉크 그대로** 쓴다: `--fg` 버킷(11)으로
   올리면 후광이 붙어 조립 중인 차가 하얗게 끓는 덩어리가 된다(실측).
5. **조립 중인 차는 쌓인 점만 그린다.** 빈 자리(`aRest`)는 그 차의 마지막 점이
   앉은 뒤에 차오르고, 앉은 점은 곧바로 차의 최종 알파로 굳는다.

검사기는 `roadProbe.morph` 의 `stay`/`air`/`land` 로 잰다 — 조립 구간에서 **셋이
동시에 0 이 아니어야** 한다. 공중 점의 `swapped` 는 0 이다.

**풀린 점은 문서를 떠나 화면에 머문다.** 여는 화면이 sticky 라 글자 자리가 화면에
고정이고, 점도 그 자리를 지킨다 — 점이 이제 문서가 아니라 **센서의 화면**에 있다.

한 단계로 만들면 안 된다. 글자가 옅어지는 것과 점이 날아가는 것이 동시에
일어나면 화면 어디에도 "글자 모양이 점으로 서 있는 순간"이 없어서, 글자가 점이
된 것이 아니라 **글자 옆에서 점이 나온 것**으로 보인다(교수님이 그렇게 읽었다).
`--ink-k` 는 ①의 길이 하나를 가리킨다 — DOM 이 꺼지는 구간과 ①은 같은 값이다.

- **한 점의 도착 시점은 그 글자의 위끝이 화면 위끝에 닿는 순간이다.** 글자는
  잘리기 전에 다 풀려 있다 — 화면 크기·언어가 달라도 저절로 맞는다. 아래끝이
  벗어나는 순간으로 잡았을 때는 워드마크가 풀리는 내내 윗부분이 화면 밖이라
  **글자가 점이 되는 장면을 볼 수 없었다.**
- **도착한 점은 `drawCar` 가 찍었을 픽셀과 같다.** hp = 1 에서 평범한 차 그리기로
  넘어갈 때 아무 일도 일어나지 않아야 한다(실측: 인계 프레임의 픽셀 변화가
  이웃 프레임과 같은 1.03%).
- **도착 전에는 도로가 비어 있다.** 글자를 안 받은 차·보행자까지 `fleetK` 로 먼저
  나타나면 "빈 도로였다가 글자가 차가 되는" 장면이 성립하지 않는다. 배역의 알파는
  `fleetG = max(fleetK, 글자 도착 e 0.7→1)` 이다 — hp = 1 에서 둘 다 1 이라 인계가
  보이지 않는다. 검사기는 `roadProbe.cars`(도로에 **몸통을 올린** 차만 센다)로
  도착 전 0 을 확인한다.
- **자리는 LOD 가 아니라 템플릿 전체다.** LOD 만큼만 주면 데스크톱 자리가
  1,950개뿐이라 글자 점을 그 이상 구울 수 없다. 글자가 내려앉는 동안 그 점들은
  이미 화면에 있으므로(글자 자리에) 아낄 것이 없다. LOD 밖의 자리는 도착 직전
  (e 0.9→1)에 꺼져 인계에 남지 않는다.
- **글자를 든 차는 아직 등장 전이라도 그린다.** `fleetK` 로 차를 걸러내면 그 차가
  들고 있던 글자 점이 하나도 안 그려진다 — 여는 화면에서 **M 만 점이 되고 A·I·A 는
  나타나지 않았다.** 반대로 글자가 떠나 있는 동안 차의 나머지 점을 지워서도 안 된다
  (`aRest` 는 차의 제 알파 아래로 내려가지 않는다).
- **받을 차는 연출이 시작될 때 정해 얼린다.** 옆 차로는 링이라 차가 카메라를
  지나가면 배정이 통째로 밀린다 — 한 프레임에 554점이 다른 차로 옮겨 붙었다.
  옆 차로는 거리 창(`GLYPH_DZ_MIN`…`GLYPH_DZ_MAX`) 안의 차에만 준다.
- 굽는 것은 **프레임 밖**이다. 리사이즈와 `document.fonts.ready` 때만 다시 굽는다
  (글꼴 전 2,000점 · 후 2,000점이지만 자리가 다르다).
- 동작 줄이기에서는 아예 굽지 않는다. `--h` 도 안 나가므로 글자는 그대로 선다.

**`hp` 의 자는 `0.85 × 창높이` 가 아니다.** 여는 화면(`.open`)의 실제 길이에 묶는다
— 무대 상단의 **문서 좌표** − 0.15H 다. 고정 자를 쓰면 여는 화면을 늘려도 연출은
그대로 첫 화면 안에서 끝나 버린다. 마크업이 척도를 정하고 훅이 따라간다.
rect 는 `approach` 를 재려고 이미 프레임에 한 번 읽는다 — 새 DOM 읽기가 없다.
무대가 없는 페이지(연구·논문…)에는 여는 화면도 없으므로 옛 자로 폴백한다.

**그래서 `.open` 은 `100dvh + max(230dvh, 123rem)` 이다**(모바일 `max(178svh, 94rem)`). 붙어 있는 상자가 그만큼
버텨야 연출(도착 1,536px)과 그 뒤 **반 화면의 주행**이 sticky 안에 들어간다 — 비율만
으로 잡으면 468px 짜리 창에서 1,076px 밖에 안 돼 조립 도중에 상자가 떠내려간다. 콘텐츠 상자는 예전 `.open`
그대로라 **첫 화면 픽셀이 동일하다**(hero:check). 그 여유는 빈 화면이 아니다: 그
구간에서 글이 점이 되어 날아가고, 도착한 뒤로는 **글자로 만들어진 차들이 달리는
것**을 본다.

**JS 가 없으면 변수도 없다.** CSS 는 반드시 폴백을 둔다(`var(--a0, 1)`) — 폴백은
"전부 보이는" 상태여야 한다.

## 연구 무대 — 세로로 지나가는 네 분야

홈의 연구 섹션은 **세로로 지나가는 네 화면**이다. 한 분야가 한 화면(`100dvh`)을
쓰고, 그 화면에서 그 분야의 실제 IDM 시뮬레이션(`TrafficSim compact`)이 돌아간다.
설명문 대신 **돌아가는 모델**이 분야를 말한다(연구분야에 설명문을 두지 않는다:
교수님 결정).

한동안 **가로로 넘기는 덱**이었다. 네 분야가 한 자리에 겹쳐 있어 공간은 아꼈지만,
"옆으로 넘길 수 있다"는 것 자체가 안 읽혔고 그걸 알리려고 ◂▸·눈금·번호를 더할수록
요소가 늘었다. 더 큰 문제는 **넘기지 않으면 셋을 못 본다**는 것이었다 — 연구실
홈페이지에서 연구 분야는 스크롤만 해도 전부 지나가야 한다. 지금은 그냥 내려간다.

- 한 화면에 두는 것은 **넷뿐이다**: 번호(`01 / 04`) · 이름 · 키워드 한 줄 · 시뮬레이션.
  거기에 배경 도로의 계측판까지 얹었더니 요소가 너무 많아졌다. 늘리기 전에 의심하라.
- 화면 밖으로 잘려 나가는 거대한 번호(`.ghost`)는 **장식이다**(`aria-hidden`). 읽는
  번호는 `Research 03 / 04` 한 줄이 따로 있다. 좁은 화면에서는 잘릴 변이 위쪽밖에
  없어 글자 반쪽이 허공에 뜨므로 **지운다**.
- 훅에는 가로 덱 경로(`[data-deck]`)가 아직 남아 있다. **지금 쓰는 곳은 없다** —
  덱을 다시 쓸 게 아니면 새 마크업에 `data-deck` 을 붙이지 마라.

### 시점은 첫 막이 다가올 때 오른다

`riseK = smooth((actT₀ + RISE_LEAD) / RISE_LEAD) × (1 − smooth((actT₃ − 0.78) / 0.72))`,
`RISE_LEAD = 1.3`.

사다리꼴이다 — 첫 막이 다가오면 오르고(대시캠 1.3m → 루프탑 8.5m), 네 막이 지나는
동안 머물고, 마지막 막이 자리를 뜨면 내려온다. 무대를 벗어나면 정확히 0 이다.

**선행은 한때 0.62 였다.** 여는 화면이 정확히 한 화면이라 무대 앞에 여유가 없었기
때문이다(0.85 로 잡으면 **스크롤 0 에서 이미 rise 0.09** — 사람 눈으로는 놓쳤고
`stage:check` 가 잡았다). 지금은 글자 연출 때문에 여는 화면이 세 화면 넘게 길어져
앞에 여유가 충분하므로 **1.3** 이다.

**그리고 시점 높이는 `stageK` 가 아니라 `riseK` 만 따른다.** `stageK = min(1, wsum)·riseK`
인데 `wsum`(막이 화면 30% 선에 다가서는 정도, `ACT_LEAD` 0.26 + `ACT_IN` 0.12)이
스크롤 330px 밖에 안 돼서, 상승이 통째로 거기 갇혀 있었다 — 실측으로 camY 가 200px
만에 **1.8m → 8.4m** 로 솟았고, 그래서 "연구 주제들이 나올 때 갑자기 다른 화면으로
넘어가듯" 보였다(교수님 지적). 막이 아직 아무도 안 섰을 때는 **첫 막의 구도**를
목표로 삼아 `riseK` 로 간다. 지금은 같은 상승이 1,350px 에 걸쳐 일어난다
(100px 당 약 1m). `back`·`panK` 는 그대로 `stageK` 다 — 자차가 보이는 것과 좌우
팬은 **그 막의** 구도이지 시점 상승이 아니다.

### 무대 위의 도로는 스크롤로 달린다

이 사이트의 규칙은 "스크롤이 멈추면 도로도 멈춘다"이고, 세로 무대는 그 규칙을
그대로 따른다. 한 화면(860px)을 지나면 **44.7m** 를 달리므로(`M_PER_PX` 0.052)
한 막의 연출 한 바퀴(`ACT_RUN` 38m)가 그 안에 들어간다 — 내려가기만 하면 파동도,
승객도, 합류도 제때 일어난다.

> 가로 덱에는 이 성질이 없었다. 패널이 제자리에 서 있어 `actT` 가 0 에 머물고
> 주행거리도 늘지 않아서, **무대 위에서만 도로가 스스로 달리는** 예외를 따로 두어야
> 했다(`selfZ`). 세로로 돌아오면서 그 예외가 필요 없어졌다. 코드에는 남아 있지만
> `deckRise ≥ 0` 일 때만 붙으므로 지금은 죽어 있다.

**검사기는 반드시 스크롤로 지나가야 한다.** 한 자리에 세워 두고 기다리면 도로가
얼어 있어 연출이 영영 일어나지 않는다 — `stage:check` 의 ② 가 그래서 한 걸음씩
실제로 스크롤하며 잰다.

### 막마다 다른 자리에 선다

같은 구도로 네 주제를 보여주면 주석만 바뀐 것으로 읽힌다(실제로 그랬다).
카메라 높이·화각·자차와의 거리를 전부 다르게 잡는다(`ACT_CAM`).

| 막 | 카메라 | 무엇이 보이는가 | 실제 시뮬인가 |
|---|---|---|---|
| 01 Mixed Traffic | 9.5m | 옆 차로 노면의 **속도 리본** — 붉은 띠가 뒤로 밀려온다. 후반에 제어 AV 3대 투입 | ○ IDM + FollowerStopper |
| 02 AV Control | 4.6m · 뒤로 22m | 차마다 속도 막대가 서고, 선두 급제동이 **뒷차로 전파**되며 막대가 차례로 주저앉는다 | ○ IDM |
| 03 Mobility Service | 3.4m | 호출 → 갓길 정차 → 승객이 걸어와 탑승 → 출발 | ✗ 안무(절차 시연) |
| 04 CAV·CDA | 7.8m · 뒤로 19m | 뒷차가 **먼저 간격을 내주고** V2V 링크가 맺힌 뒤 합류 | ○ IDM(양보는 override) |

### 도로 위의 차는 17대다 — 늘리기 전에 화면을 봐라

자차 + 앞차 4(`FLEET_N`) · 옆 차로 링 7(`NEXT_N`) · 마주 오는 두 줄 3+3(`ONC_N`).
한때 23대였고 "배경에 차가 너무 많다"는 말을 들었다. 가장 붐비던 곳은 **마주 오는
두 줄**(24m 간격)이라 거기부터 줄였다 — 지금은 40m 간격, 8초에 한 대가 스친다.

01막의 속도 막대는 `BAR_NEAR`(16m)보다 가까운 차에는 그리지 않는다. 가까이서는
2m 남짓한 기둥이 화면 높이의 3분의 1을 차지해, 차에 붙은 계측이 아니라 **도로에
꽂힌 초록 선**으로 보인다. 막대의 뜻은 여러 개를 나란히 놓고 높이를 비교하는 데 있다.

### 막 안의 진행도는 `actT` 가 아니라 `actU` 다

`actU` 는 막 안에서의 **연출 재생 헤드**다. 세로 무대에서는 `actT` 와 같고(스크롤이
곧 진행도다), 가로 덱에서는 주행거리로 따로 흘렀다.

- `actU` 가 한 바퀴를 돌면 배역(제어 AV·택시·합류차)도 **같이 놓아 준다.** 진행도만
  0 으로 돌리면 배역은 끝 상태에 남아 다음 바퀴 첫 프레임에 차가 순간이동한다.
- 무대를 벗어나면 `actU` 를 0 으로 되돌린다. 그래야 다시 들어왔을 때 처음부터 흐른다.

### 막의 세기(`actK`)와 화면 위치(`actRaw`)는 다른 값이다

| 값 | 무엇인가 | 누가 쓰는가 |
|---|---|---|
| `actRaw[i]` | **화면 위치**만. 그 막이 지금 화면의 어디에 있는가 | `--a0`…`--a3`(CSS) · 카메라 구도 혼합비 |
| `actK[i]` | 무대에 서 있을 때만 존재하는 **연출의 세기** | 캔버스 주석 · 배역 캐스팅 · `actU` |

가로 덱에서는 **스크롤 0 에서도 첫 패널이 제자리에 있어서** 위치만 보면 01막이
만개한 상태였고, 그 값을 연출에 그대로 썼더니 히어로 한복판에 01막의 속도 막대가
통째로 꽂혔다("도로에 웬 초록 선"). 그래서 둘을 갈랐다. 세로 무대에서는 막이 화면에
들어와야 값이 오르므로 이 함정이 없지만, 두 값의 구분은 그대로 둔다 —
`actRaw` 는 구도를, `actK` 는 연출을 뜻한다.

카메라 혼합비에 `riseK` 를 곱하지 마라. `stageK` 에서 한 번 더 곱해져 `riseK²` 이
되고, 카메라가 무대 한참 안쪽에 들어와서야 뜬다.

### 수치는 시뮬레이션 한 곳에서만 잰다

홈 패널에 배경 도로의 실시간 계측판(`ACT_READOUT` / `data-act-gauge`)을 얹었던 적이
있다. 같은 화면의 두 계측이 **같은 이름의 값을 서로 다른 수로** 내놓아(시뮬은 검증된
12대·54km/h, 배경 도로는 장면인 6대·12km/h), 읽는 사람이 둘을 맞춰 보려 했다.
계측은 `TrafficSim` 한 곳에서만 한다. 배경 도로의 값은 검사기용 `roadProbe` 로만 나간다.

그때 확인한 것 하나는 남겨 둔다. **검증표의 상용 ACC 증폭 1.30× 는 12대·54km/h 의
성질이고, 배경 도로의 6대·12km/h 정체류에서는 재현되지 않는다** — 오프라인으로
확인했더니 교란이 세 대쯤에서 잦아들었다.

> 재현되지 않는 수치를 화면에 띄우는 것은 디자인 문제가 아니라 **거짓**이다.
> 수치를 새로 띄우려면 먼저 그 조건에서 재현되는지 오프라인으로 확인하라.

같은 이유로 `stage:check` 는 02막에서 **깊이가 아니라 전파**를 잰다. 교란이 몇 대
뒤까지 가는지를 요구하는 판정을 두었더니, 대열이 우연히 압축된 순간에만 통과하고
평소에는 실패했다 — 재현되지 않는 것을 요구하는 검사는 검사가 아니다.

### 계측기의 크롬 — 눈썹줄 없음 · 문장 케이스 · 숫자만 모노

시뮬레이션 **둘레**(제목·수치판·상태줄·라벨·틀)가 "AI 가 만든 대시보드"로 읽힌다는
말을 들었다. 애니메이션이 아니라 크롬이 문제였다. 걷어낸 것과 그 자리에 둔 규칙:

| 걷어낸 표식 | 지금 |
|---|---|
| 틀 위의 눈썹줄 `SIM · SHOCKWAVE` | 없음. 제목 위의 키커는 금지 항목이고, 내부 시나리오 키를 화면에 노출하고 있었다. 코너 틱·헤어라인은 카드 어휘라 남는다 |
| `AV PENETRATION → WAVE DAMPING` | 이 계측기가 무엇을 보여주는지 적는 **한 문장**(`SIM_LEGEND.readout`). 본문 글꼴·문장 케이스·마침표 없음. 홈(`compact`)에는 싣지 않는다 — 거기서는 설명문으로 읽힌다 |
| 큰 숫자 4칸(라벨 대문자 + 큰 수치 + 단위) | **계측기 한 줄의 텔레메트리.** 라벨은 문장 케이스 sans, 값만 모노 tabular, 단위는 SI 표기 그대로(`m/s`·`km/h`). 결론값(첫 항목)은 크기가 아니라 **굵기 한 단계**로 선다 |
| `■ SETTLING · 2-LAP WINDOW 8/104 S` | 사람 말 + 로케일(`Settling · 8 of 104 s in the two-lap window` / `안정화 중 · 두 바퀴 측정 창 104초 가운데 8초`). 표시등은 유니코드 글리프가 아니라 **CSS 로 그린 6px 사각 틱** — 측정 중에는 차 있고 확정되면 테두리만 남는다 |
| `22 VEH · RING 400 M · FOLLOWERSTOPPER AV × 0` | 문장으로. 모노로 남은 이유는 거기 실린 것이 수치와 단위이기 때문이다 |
| 링 옆의 축 없는 스파크라인 | **시공간도**(다음 절). 한때 축을 붙인 속도 편차 띠로 고쳤는데, 축이 생겨도 그것은 파생 지표라 장면과 이어지지 않았다 — 지금 그 자리는 원자료가 쓴다 |

**대문자 + 자간은 버튼(`.ctrl`)만이다.** 하드 룰 4 는 버튼에 관한 규칙이지 모노
전체에 관한 규칙이 아니다 — 그 값이 라벨·상태·구성 표기까지 번지면 한 줄이 계측
표기가 아니라 **기술적인 척하는 costume** 이 된다.

수치의 **종류**는 그대로다. 표기만 바꿨다 — 새 계측을 만들면 "같은 이름의 값을 다른
수로" 내는 화면이 다시 생긴다(바로 위 절). `sim-check` 는 `dl`·`[class*=readings] p`·
`data-state` 로 읽으므로 그 셋은 구조를 바꿀 때 함께 본다.

### 시뮬 그림은 논문의 그림이다

크롬을 걷어낸 뒤에도 "애니메이션 **자체**가 AI 스럽다"는 말이 남았다. 맞는 지적이었다.
점선 원 위를 도는 같은 크기의 네모, 차마다 뻗는 방사형 꼬리, 이퀄라이저 같은 세로 막대
12개, 점선 격자 위에 빛나는 사각형 — 교통을 아는 사람의 그림이 아니라 생성형 이미지의
**"미래 교통 HUD" 상투구**였다. 축척도 차로도 없었다.

지금 네 장면은 전부 **장면(평면도) + 그 분야의 표준 도표** 한 쌍이다.

| 막 | 장면 | 도표 |
|---|---|---|
| 01 platoon | 직선 차로 평면도 — 폭이 있는 노면, 실제 비율(4.6 × 1.8 m)의 차, 25·100 m 눈금자 | **v(t) 프로파일** — 한 주기(45 s) 동안 12대의 속도 곡선. 뒤로 갈수록 골이 깊어지면 그게 증폭이다(string stability 논문의 그림) |
| 02 shockwave | 링 조감 평면도 — 가장자리 실선 두 겹, 접선 방향의 직사각형 | **시공간도** — 가로 60 s × 세로 400 m, 색은 속도. 정체 파동이 **뒤로 밀려가는 붉은 띠**로 보인다(Sugiyama 2008 · Stern 2018) |
| 03 dispatch | 도시의 지도 — 테두리를 전부 먼저 긋고 노면으로 덮으면 교차로가 저절로 뚫린다 | 없다. 지도가 폭을 다 쓴다 |
| 04 v2v | 본선 + 합류 램프 평면도 | 없다. 링크 1px 실선과 양보 간격 브래킷이 장면 안에 있다 |

규칙 넷:

1. **도표는 원자료다.** 위치·속도·시간을 그대로 찍는다. "속도 편차의 시계열" 같은
   파생 지표는 축을 붙여도 장면과 이어지지 않아 빈 자리를 채우는 장식으로 읽힌다.
   속도 편차는 지금 **확정 판정에만** 쓰이고 화면에는 나가지 않는다.
2. **점선은 장식이 아니다.** 점선은 이 사이트의 구분선 어휘다 — 도로도 축도 실선이고,
   원은 `arc()` 금지라 64각형의 현(弦)으로 잇는다. 선은 회전한 `fillRect` 하나다.
3. **색은 속도다.** 네 장면이 같은 램프를 쓴다(`--i-6` 정지 → `--i-2` 자유주행, 12버킷,
   버킷당 `fillStyle` 하나). 차종·배정·제어 여부는 색이 아니라 **1px 테두리**로
   지목한다 — 색이 두 가지를 뜻하기 시작하면 둘 다 안 읽힌다.
4. **캔버스에 글자는 없다**(aria-hidden). 눈금의 뜻은 HTML 한 줄이 맡는다 — 새 줄을
   만들지 말고 이미 있는 구성 표기(`SETUP`)에 한 절을 붙인다.

**좁은 화면에서는 세로로 쌓는다.** 01·02 는 한 캔버스에 그림이 둘(장면 + 도표)이라
가로가 좁으면 나란히 놓을 수 없다 — 위에 작은 링, 아래에 폭을 다 쓰는 시공간도다.
그만큼 캔버스가 높아야 하므로 높이는 시나리오마다 다르다(`.canvas[data-sim=…]`,
02 는 300px · 01 은 264px · 03·04 는 176px 그대로). **홈(`compact`)은 예외로 176px 이고
그 높이에서는 장면만 그린다** — 거기서 계측기는 번호·이름·키워드와 함께 한 화면(act)
안에 서므로 높이를 늘리면 그 화면이 넘치고, 축의 뜻을 적는 구성 표기 줄도 애초에 없다.

시공간도는 매 프레임 60초 × 22대를 다시 찍지 않는다. 0.25초마다 `ImageData` 에 **열
하나**를 쓰고(`sampleSpace`), 그리기는 링 버퍼를 두 조각으로 옮기는 `drawImage` 2회다.
한 열에서 **직전 열의 행까지 이어 칠한다** — 점만 찍으면 궤적이 파선으로 끊긴다.
그리고 **warmup 90초 동안에도 채운다.** 안 그러면 화면에 들어선 뒤 40초 동안 빈 격자를
보게 되고, 그 사이에는 이 그림이 무슨 말을 하려는지 알 수가 없다.

그리기 비용은 `canvas.simProbe`(EMA·최대, ms)로 읽는다 — `roadProbe` 와 같은 자리다.
실측(1103×300, DPR 1): 01 은 0.75/1.0 · 02 는 0.29/0.8 · 03 은 0.17/0.5 · 04 는
0.07/0.3 ms. 도표를 더 얹기 전에 여기를 먼저 본다.

### 열린 대열에서 희망속도를 흔들지 마라

`driver()` 는 차두시간 T 와 정지간격 s0 를 흩뜨리되 **희망속도 v0 는 전원 같게** 준다.
링(옆 차로·마주 오는 차로)에서는 v0 를 흔들어도 총 둘레가 고정이라 갇히지만, 자차
앞의 **열린 플래툰**에서는 선두가 자기 v0 로 자유주행하고 뒷차 v0 가 낮으면 차간이
끝없이 벌어진다. 페이지를 한참 열어 두면 선두가 40m 앞에 가 있어 급제동을 걸어도
뒷차가 반응할 이유가 없었다 — 02막의 연쇄가 통째로 사라졌다(gap=[14 12 15 **40**]).

정지간격 편차도 **사람과 ACC 를 나눈다.** 사람은 0.5~2.5배로 크게 다르지만 ACC 는
설정 간격을 지킨다(0.72~1.45배). 플래툰에 사람 폭을 주면 차간이 21m 까지 벌어져
선두 급제동이 두 대 뒤에서 흡수된다.

### 막은 번호가 아니라 **이름**으로 지목한다

화면에 서는 순서는 `src/data/research.ts` 의 `RESEARCH` 배열이 정하고, **그 순서는
바뀐다**(01·02 를 맞바꾼 적이 있다). 연출을 `actK[0]` 처럼 숫자로 지목해 두면 순서를
바꾸는 순간 속도 막대가 혼합교통 막에서 서고 리본이 제어 막에서 깔린다.
`ACT_MIX` · `ACT_PLATOON` · `ACT_SERVICE` · `ACT_CDA` 로 지목한다.

순서를 바꿀 때 **함께 고쳐야 하는 네 곳**:

1. `RESEARCH` 배열 순서와 각 항목의 `index`('01'…)
2. `usePointCloud.ts` 의 `ACT_*` 상수 값
3. `ACT_CAM` 표의 순서(막마다 카메라가 다르다)
4. `stage-check.mjs` 의 `PLATOON_PANEL`(제동 연쇄를 그 구역에서만 잰다)

### 링 시뮬의 배열 순서를 신뢰하지 마라

`step()` 은 링 도로에서 차량이 0 을 넘어갈 때마다 배열을 회전시킨다
(idm.ts `rotateToOrigin`). 차종·합류차·제어 AV 를 **배열 인덱스로 지목하면**
한 바퀴마다 다른 차를 가리킨다 — 제어 AV 3대가 6대로 불어난 적이 있다.
`veh.id` 로 지목하고, 부속 배열(`shape`·`jog`)도 `veh.id` 로 색인한다.

### 03 배차는 **도로망**이다 — 회로가 아니다

한때 직사각 회로 하나였다. 차가 한 줄로 돌기만 해서 "배차"가 아니라 **순환버스**로
읽혔고, 캔버스에서는 긴 가로줄 두 개로 보였다. 더 근본적인 문제는 배차의 본질 —
**길이 갈라지는 곳에서 어디로 갈지 고르는 것** — 이 회로에는 아예 없었다는 점이다.

지금은 교차로 18곳(6×3, 간선 두 개를 덜어냄)짜리 격자다. 수요가 교차로에 뜨면
**도로를 따라** 가장 가까운 유휴 차량이 최단경로(전 쌍 다익스트라, 빌드 시 1회)로
향하고, 태운 뒤 목적지로 간다. 차량은 간선 위에서 IDM 으로 앞차를 따르되
**다음 간선의 맨 뒤차까지** 본다 — 교차로 너머를 안 보면 정지선 앞에서 뒤차가 들이받는다.

여기서 실제로 밟은 지뢰 넷:

- **IDM 은 정지간격(s0 = 2m) 앞에서 선다.** 그래서 "노드를 지나면 도착"으로 판정하면
  그 순간이 **영영 오지 않는다.** 전 차량이 목표 2m 앞에 굳었고 `TRIPS SERVED` 가
  0 에서 안 움직였다. 도착은 **서 있는 것으로** 판정한다.
- **유휴 차량도 다음 간선을 미리 정해 둬야 한다.** 경로가 비어 있으면 "목표에 도착
  중"으로 취급되어 **모든 교차로가 정차 지점**이 된다. 전 차량이 그렇게 멈춰 섰다.
- **회전 감속은 필요한 제동거리를 역산해서 건다.** 교차로 앞 고정 거리에서 제동을
  시작하게 했더니 그 거리가 제동거리보다 짧아 상시 제동이 됐다(평균 14km/h).
- **차량 수 ≥ 동시 수요 상한이면 '기다리는 수요'가 화면에 안 남는다.** 뜨는 즉시
  배차되기 때문이다. 기다리는 마커가 커지는 것이 이 장면의 핵심이므로 차를 하나 모자라게 둔다.

그리는 쪽에서는 **가로·세로 축척을 따로** 잡는다(비는 1.75배까지). 계측기 캔버스의
가로세로비가 페이지마다 다르기 때문이다(연구 페이지 3.8, 홈 패널 2.6) — 한 축척으로
맞추면 한쪽에서 화면의 절반이 빈다. 도시의 블록은 원래 정사각형이 아니라서 늘어난
격자도 도시로 읽힌다. 차량 각도는 **화면 좌표**에서 다시 재야 한다.

장면은 **45초를 미리 돌려 놓고 시작한다.** 빈 도로에서 시작하면 첫 운행이 끝나기까지
20초가 걸리고, 그 사이 방문자가 보는 것은 `TRIPS SERVED 0` 뿐이라 "배차가 안 되는
시스템"으로 읽힌다. 수치는 그대로 실측이다 — 과도구간을 건너뛴 것이지 지어낸 게 아니다.

### 차량 겹침

서로 다른 시뮬(플래툰·옆 차로 링·연출 차량)이 같은 공간을 쓰므로 겹칠 수 있다.
`roadProbe.overlap` 이 매 프레임 차체 상자 침투를 세고, `stage:check` 가 0 을 요구한다.
연출 차량은 `reserve()` 로 자리를 예약하고 시뮬 차량이 비켜 준다. 합류는
**종방향을 먼저 맞추고 그 다음에 차로를 바꾼다** — 두 축을 같이 움직이면 스친다.

### 그 밖에

- 시점을 올리고 내리는 구간은 `usePointCloud.ts` 의 `riseK` 한 곳이 정한다.
- 무대의 길이는 `HomeBody.astro` 의 `.area { min-block-size }` 가 정한다(한 분야 한 화면).
- **`FLEET_V` 는 화면 속도가 아니라 시뮬 시계의 속도다.** 시뮬 시각 = 주행거리 / `FLEET_V`.
- **프리셋의 `v0`(희망속도, 30 m/s)를 장면 속도로 덮어쓰는 것을 잊지 마라.** 한 번
  빠뜨렸더니 정체류 대열이 108 km/h 를 향해 가속해 카메라가 도로보다 빨리 달아났다.

## 배포

정적 산출물이라 어디든 올라가지만, **도메인 루트에 놓이는지 하위 경로에 놓이는지**가
코드에 영향을 준다.

| 호스팅 | `SITE_URL` | `BASE_PATH` |
|---|---|---|
| GitHub Pages · 계정 페이지 | `https://<계정>.github.io` | 비움 |
| GitHub Pages · 프로젝트 저장소 | `https://<계정>.github.io/<저장소>` | `/<저장소>` |
| Cloudflare Pages · Netlify | `https://<프로젝트>.pages.dev` | 비움 |
| 학교 도메인 | `https://maia.mju.ac.kr` | 비움 (+ `public/CNAME`) |

둘 다 GitHub 저장소 변수(Settings → Secrets and variables → Actions → Variables)로
넣는다. 워크플로가 빌드 환경변수로 넘긴다.

**하위 경로 배포에서 링크가 깨지지 않게 하는 것은 `src/i18n/index.ts` 세 함수다.**

- `href()` · `asset()` — 주소를 **만들 때** base 를 붙인다
- `stripBase()` — 주소를 **읽을 때** base 를 뗀다 (`langFromPath`·`routeKey` 가 먼저 거친다)

이 둘은 **짝이다.** 한쪽만 고치면 언어 토글이 자기 자신을 가리키거나 404 로 간다.
새 링크를 만들 때 `/publications` 같은 문자열을 직접 쓰지 말고 반드시 `href()` 를,
`public/` 자산은 `asset()` 을 거친다. 확인은 빌드로 한다:

```bash
BASE_PATH=/maia-lab SITE_URL=https://example.github.io/maia-lab npm run build
grep -oE 'href="/[^"]*"' dist/ko/people/index.html | sort -u   # 전부 /maia-lab/ 로 시작해야 한다
npm run build                                                   # 끝나면 루트로 되돌린다
```

공개 페이지에는 **기관 연락처만** 싣는다. 기존 Wix 사이트의 개인 휴대번호가 푸터에
그대로 옮겨져 있었고 뺐다 — 정적 사이트는 통째로 긁히므로 한 번 공개되면 되돌릴 수 없다.

## 비밀값

**이 사이트에는 비밀이 없다.** 환경변수는 `SITE_URL`·`BASE_PATH` 둘뿐이고 둘 다 주소다.
아래 장치는 **나중에 생길 때** 새지 않게 하려고 있다.

> **정적 사이트는 비밀을 지킬 수 없다.** 브라우저에 도달하는 값은 무엇이든 방문자가
> 본다 — 난독화해도, 빌드로 감춰도 마찬가지다. 키가 필요한 호출은 서버리스 함수를
> 하나 두고 거기서 한다. Astro/Vite 에서 `PUBLIC_` 접두사는 **번들에 그대로 박힌다** —
> 이 계열에서 키가 새는 1번 경로다.

| 층 | 무엇을 막나 |
|---|---|
| `.gitignore` 의 `.env.*` + `!.env.example` | `.env.local`·`.env.bak` 같은 변종까지 전부 |
| `npm run lint:secrets` (verify·CI 에 포함) | 소스에 박힌 키를 **커밋 전에** 잡는다 |
| GitHub 비밀값 스캐닝 + **푸시 차단** | 그래도 새어 나간 것을 **푸시 시점에** 막는다 |

`scripts/lint-secrets.mjs` 는 git 이 **추적하는** 파일만 본다 — 추적되는 것이 곧
푸시되는 것이고, 푸시된 비밀은 되돌려도 히스토리·포크·캐시에 남는다.
규칙을 추가할 때는 오탐을 경계하라. 오탐이 잦으면 사람이 검사를 무시하게 되고,
그러면 검사가 없는 것만 못하다.

## CSP · 공급망

- **CSP 는 Astro 가 해시로 만든다**(`security.csp`, astro.config.mjs). 인라인
  스크립트마다 해시가 박히므로 `'unsafe-inline'` 이 없다 — 주입된 스크립트는
  실행되지 않는다. 외부 리소스를 하나도 쓰지 않으므로 전부 `'self'` 다.
  외부 스크립트를 넣게 되면 여기서 명시적으로 열어야 한다.
- **CSP 는 `astro dev` 에 없다. `npm run csp:check` 로 본다.**
  `<meta>` 는 `astro build` 만 만든다. dev 서버로 확인하면 위반 0건이 나오고
  배포하면 조용히 깨진다 — 실제로 그렇게 나갔다. `style-src` 에 해시가 있으면
  규격상 `'unsafe-inline'` 이 무시되고 인라인 `style=""` **속성**은 해시를 붙일
  자리가 없어 통째로 막힌다. `--k`(덱 위치 눈금)·초상 비율·히어로 타이핑이
  전부 죽었는데 **콘솔에는 아무것도 안 찍혔다.** 사람이 화면을 보고 잡았다.
  그래서 `csp:check` 는 위반 이벤트만 보지 않고 **결과 값**을 직접 확인한다
  (눈금이 현재 칸만 가리키는가). 인라인 속성은 `styleDirective.resources` 의
  `kind: 'attribute'` 로 연다 — 스크립트는 해시로 잠긴 채 남는다.
- `frame-ancestors` 는 **일부러 넣지 않았다.** `<meta>` 로는 브라우저가 무시하는
  지시어라, 넣으면 보호받는다고 착각하게 된다. 응답 헤더로만 가능하고 GitHub
  Pages 는 헤더를 못 넣는다.
- **액션은 커밋 SHA 로 고정한다.** 태그는 저장소 관리자가 옮길 수 있다. 갱신은
  Dependabot 이 PR 로 올린다(`.github/dependabot.yml`) — 고정과 갱신은 한 쌍이다.
- **이 저장소는 평소에 아무도 보지 않는다.** 연구실 홈페이지 하나를 올려 두는
  곳이지 개발팀이 붙은 저장소가 아니다. 알림·PR 을 늘리는 설정을 넣기 전에
  "이걸 누가 볼 것인가"를 먼저 물어라. 아무도 안 보는 PR 이 쌓이면 그건 보안이
  아니라 소음이고, 소음이 쌓이면 정작 중요한 PR 도 같이 묻힌다. 그래서 일반
  npm 버전 갱신은 끄고 **실제 취약점만** GitHub 이 그때그때 올리게 해 두었다.
- 워크플로 기본 권한은 `contents: read`. 필요한 권한은 각 작업이 스스로 선언한다.

## 참고

- 디자인 템플릿 54종: `~/.claude/skills/popular-web-designs/templates/`
  (주력: `x.ai.md`, `linear.app.md` §5, `voltagent.md`, `spacex.md`, `bmw.md`)
- [Astro 라우팅](https://docs.astro.build/en/guides/routing/) ·
  [프레임워크 컴포넌트](https://docs.astro.build/en/guides/framework-components/) ·
  [i18n](https://docs.astro.build/en/guides/internationalization/)
